version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/db/cinema.db
      - IMPORT_SCHEDULED_ENABLED=true
    volumes:
      - ./db:/app/db:rw
